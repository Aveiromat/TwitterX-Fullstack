# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Declare files that will always have CRLF line endings on checkout.
*.bat text eol=crlf
*.cmd text eol=crlf

# Declare files that will always have LF line endings on checkout.
*.sh text eol=lf
*.yml text eol=lf

# Denote all the files that are truly binary and should not be modified.
*.jpg binary
*.png binary
*.gif binary

# Handle line endings for common programming languages.
*.c text
*.cpp text
*.cs text
*.h text
*.java text
*.js text
*.json text
*.py text
*.rb text
*.xml text
*.html text
*.css text
*.scss text

# Custom attributes for specific files or directories.
# For example, set all files in 'docs' to always use LF line endings.
docs/* text eol=lf

# Handle markdown files
*.md text eol=lf
